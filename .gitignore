__pycache__/
*.py[cod]
sim_results/

# Локальные резервные копии БД (создаются seed-скриптом перед записью)
resources/*.db.bak-*

# Claude Code local tooling (agent worktrees, session settings)
.claude/
